# infrastructure

This package handles the resources for a deployment.

Responsibilities:

- create isolated deployment environments
- provision cloud account or project resources
- manage ingress, DNS, secrets, and related dependencies
- expose a narrow interface for core and sync
